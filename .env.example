# App
PORT=
APP_URL=http://localhost:${PORT}

# Database
DB_USERNAME=
DB_PASSWORD=
DB_PORT=
DB_NAME=
DB_URL=postgresql://${DB_USERNAME}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}?sslmode=disabled

# JWT
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=
JWT_REFRESH_EXPIRES_IN=

# Resend
RESEND_API_KEY=
